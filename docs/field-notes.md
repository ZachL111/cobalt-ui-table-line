# Field Notes

`cobalt-ui-table-line` is easiest to review by starting with the fixture, not the prose.

The domain cases cover `view drift`, `state pressure`, `layout risk`, and `interaction cost`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

`recovery` tells me the happy path still works. `baseline` tells me whether the guardrail still has teeth.

The point is not to make the repository bigger. The point is to make the important judgment testable.
